# Data Dictionary

All sample files under `data/sample/` are synthetic and exist only to demonstrate close-control workflows.

The canonical schema definitions are maintained in `src/finance_close_control_tower/schema.py`.
