# Scoring Model

The MVP scoring model will use a transparent weighted score out of 100 across trial balance integrity, reconciliation completion, suspense ageing, bank reconciliation, VAT control movement, and intercompany matching.

Phase 0/1 does not implement scoring yet. The public version will keep score logic deterministic and explainable while leaving deeper commercial calibration outside the portfolio repository.
